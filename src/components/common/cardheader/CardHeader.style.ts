// Vendors
import { styled } from '@mui/material/styles';

export const Wrapper = styled('div')({
  display: 'flex',
  marginBottom: '10px',
  alignItems: 'center',
  h5: { color: '#ffffffe6', flexGrow: 1 },
});
